package tgpr.bank.model;

public class Administrator extends User {

    public Administrator(Integer id, String type, String email, String password, String lastname) {
        super(id, type, email, password, lastname);
    }
}
